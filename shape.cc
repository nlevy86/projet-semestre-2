#include <iostream>
#include <cmaths>
#include "shape.h"
#include "constantes.h"
#include "message.h"

constexpr double epsil_zero(0.5) ;

using namespace std;

void shape::setbase(abs,ord){
  if ((ab>=1 and ab<=max-1) and (or>=1 and or<=max-1){
    S2d.x=ab;
    S2d.y=or;
  } else { 
};

void shape::setangle(rad){
  angle=rad;
};

void shape::setlongueur(taille){
  longueur=taille;
};
